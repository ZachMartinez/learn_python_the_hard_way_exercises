# Prints "I will now count my chickens:" in the terminal.
print("I will now count my chickens:")

# Prints "Hens" followed by an evauation of the second parameter.
print("Hens", 25.0 + 30 / 6)
# Prints "Roosters", followed by an evaluation of the second parameter.
print("Roosters", 100.0 - 25 * 3 % 4)

# Prints the message "Now I will count the eggs:" in the terminal.
print("Now I will count the eggs:")

# Prints an evaluation of the statement in the parentheses.
print(3.0 + 2 + 1 - 5 + 4 % 2 - 1 / 4 + 6)

# Prints message in the terminal
print("Is it true that 3 + 2 < 5 - 7?")

# Prints evauation of the statement in the parentheses.
print(3.0 + 2 < 5 - 7)

# Prints question in the terminal followed by the answer evaluated from the
# second parameter.
print("What is 3 + 2?", 3.0 + 2)
# Prints question in the terminal followed by the answer evaluated from the
# second parameter.
print("What is 5 - 7?", 5.0 - 7)

# Prints message in the terminal.
print("Oh, that's why it's False.")

# Prints message in the terminal.
print("How about some more.")

# Prints a messgae followed by a value evaluated from the expression in the
# parameter.
print("Is it greater?", 5.0 > -2)
# Prints a messgae followed by a value evaluated from the expression in the
# parameter.
print("Is it greater or equal?", 5.0 >= -2)
# Prints a messgae followed by a value evaluated from the expression in the
# parameter.
print("Is it less or equal?", 5.0 <= -2)
